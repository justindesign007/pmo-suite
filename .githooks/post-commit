#!/bin/sh
if [ "$PMO_SKIP_META_AMEND" = "1" ]; then
  exit 0
fi

node scripts/update-release-meta.mjs

if ! git diff --quiet -- src/meta.js; then
  git add src/meta.js
  PMO_SKIP_META_AMEND=1 git commit --amend --no-edit --no-verify
fi
